[
  {
    "id": "ch16-q1",
    "topic": "listOf",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val colors = listOf(\"red\", \"green\", \"blue\")\nprintln(colors[1])\nprintln(colors.size)",
    "question": "What does this print?",
    "choices": [
      "red then 3",
      "green then 3",
      "green then 2",
      "Compilation error"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "`listOf` creates a read-only list. Elements are accessed by 0-based index. `size` gives the element count.",
      "lineByLine": [
        "`colors[1]` — index 1 is the second element: \"green\".",
        "`colors.size` — 3 elements.",
        "Output: green, then 3."
      ],
      "tip": "`listOf` returns an immutable list — you can read from it but not add or remove elements. For a list you need to modify, use `mutableListOf`."
    }
  },
  {
    "id": "ch16-q2",
    "topic": "mutableListOf",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val nums = mutableListOf(1, 2, 3)\nnums.add(4)\nnums.remove(2)\nprintln(nums)",
    "question": "What does this print?",
    "choices": [
      "[1, 2, 3, 4]",
      "[1, 3, 4]",
      "[1, 2, 4]",
      "[1, 3]"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "`mutableListOf` creates a list you can add to and remove from. `remove(2)` removes the element with value 2 (not the element at index 2).",
      "lineByLine": [
        "Start: [1, 2, 3].",
        "`add(4)` → [1, 2, 3, 4].",
        "`remove(2)` — removes the element equal to 2 → [1, 3, 4].",
        "`println([1, 3, 4])` — prints [1, 3, 4]."
      ],
      "tip": "Watch out: `remove(value)` removes by value, while `removeAt(index)` removes by position. Confusing these is a common AI-generated bug."
    }
  },
  {
    "id": "ch16-q3",
    "topic": "mapOf",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val ages = mapOf(\"Alice\" to 30, \"Bob\" to 25)\nprintln(ages[\"Alice\"])\nprintln(ages[\"Charlie\"])",
    "question": "What does this print?",
    "choices": [
      "30 then null",
      "30 then 0",
      "30 then Charlie",
      "Throws NoSuchElementException"
    ],
    "correctIndex": 0,
    "explanation": {
      "summary": "Map lookup with `[]` returns the value if found, or `null` if the key doesn't exist. It does not throw an exception.",
      "lineByLine": [
        "`ages[\"Alice\"]` — key exists, returns 30.",
        "`ages[\"Charlie\"]` — key not found, returns null.",
        "Output: 30, then null."
      ],
      "tip": "Map lookup with `[]` always returns a nullable type. In AI code you'll see `ages[\"Alice\"] ?: 0` to provide a default when the key is missing."
    }
  },
  {
    "id": "ch16-q4",
    "topic": "setOf",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val tags = setOf(\"kotlin\", \"android\", \"kotlin\", \"compose\")\nprintln(tags.size)",
    "question": "What does this print?",
    "choices": [
      "4",
      "3",
      "2",
      "Compilation error — duplicate elements"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "A Set automatically removes duplicate values. The duplicate \"kotlin\" is ignored, leaving 3 unique elements.",
      "lineByLine": [
        "`setOf(\"kotlin\", \"android\", \"kotlin\", \"compose\")` — 4 values provided.",
        "Duplicates removed: \"kotlin\" appears once.",
        "Set: {kotlin, android, compose} — 3 elements.",
        "`tags.size` = 3."
      ],
      "tip": "Sets are used in AI code when uniqueness matters — tracking visited IDs, deduplicating tags, or checking membership. `in` operator with sets is O(1) lookup."
    }
  },
  {
    "id": "ch16-q5",
    "topic": "iterating a map",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val scores = mapOf(\"Alice\" to 95, \"Bob\" to 87)\nfor ((name, score) in scores) {\n    println(\"$name: $score\")\n}",
    "question": "What does this print?",
    "choices": [
      "Alice then Bob",
      "Alice: 95 then Bob: 87 (each on its own line)",
      "(Alice, 95) then (Bob, 87)",
      "Compilation error"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "`for ((key, value) in map)` destructures each map entry into its key and value.",
      "lineByLine": [
        "`for ((name, score) in scores)` — each entry is destructured.",
        "First iteration: name=\"Alice\", score=95 → prints Alice: 95.",
        "Second: name=\"Bob\", score=87 → prints Bob: 87."
      ],
      "tip": "Destructuring map entries in a `for` loop is one of the most common Kotlin patterns. Without destructuring, you'd write `for (entry in scores)` and use `entry.key` and `entry.value`."
    }
  },
  {
    "id": "ch16-q6",
    "topic": "in operator for collections",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val primes = listOf(2, 3, 5, 7, 11)\nprintln(5 in primes)\nprintln(4 in primes)",
    "question": "What does this print?",
    "choices": [
      "true then true",
      "true then false",
      "false then true",
      "false then false"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "The `in` operator checks if a value is present in a collection. It uses `equals()` for comparison.",
      "lineByLine": [
        "`5 in primes` — 5 is in the list → true.",
        "`4 in primes` — 4 is not in the list → false.",
        "Output: true then false."
      ],
      "tip": "`in` is the clean Kotlin way to check membership. For `List`, it's O(n). For `Set`, it's O(1). AI code uses `in` for readable guard checks: `if (status in validStatuses)`, `if (id in cache)`."
    }
  },
  {
    "id": "ch16-q7",
    "topic": "getOrDefault",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val scores = mapOf(\"Alice\" to 95, \"Bob\" to 87)\nprintln(scores.getOrDefault(\"Alice\", 0))\nprintln(scores.getOrDefault(\"Carol\", 0))",
    "question": "What does this print?",
    "choices": [
      "95 then null",
      "95 then 0",
      "0 then 0",
      "Compilation error"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "`getOrDefault(key, default)` returns the value if the key exists, or the default value if it doesn't. Unlike `[]`, it never returns null.",
      "lineByLine": [
        "`getOrDefault(\"Alice\", 0)` — Alice exists, returns 95.",
        "`getOrDefault(\"Carol\", 0)` — Carol not found, returns 0.",
        "Output: 95 then 0."
      ],
      "tip": "`getOrDefault` is cleaner than `scores[\"Carol\"] ?: 0` when you want a non-null fallback. AI code uses it for scoring, counting, and any map lookup where a missing key has a sensible default."
    }
  },
  {
    "id": "ch16-q8",
    "topic": "mutableMapOf",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val counts = mutableMapOf<String, Int>()\ncounts[\"a\"] = 1\ncounts[\"b\"] = 2\ncounts[\"a\"] = counts.getOrDefault(\"a\", 0) + 1\nprintln(counts[\"a\"])\nprintln(counts.size)",
    "question": "What does this print?",
    "choices": [
      "1 then 2",
      "2 then 2",
      "2 then 3",
      "Compilation error"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "`mutableMapOf` creates a map you can add to and update. Assigning an existing key overwrites its value.",
      "lineByLine": [
        "`counts[\"a\"] = 1` — adds key \"a\" with value 1.",
        "`counts[\"b\"] = 2` — adds key \"b\" with value 2.",
        "`counts[\"a\"] = getOrDefault(\"a\",0) + 1` = 1 + 1 = 2 → updates \"a\" to 2.",
        "`counts[\"a\"]` = 2. `counts.size` = 2 (keys: a and b)."
      ],
      "tip": "The `count[key] = count.getOrDefault(key, 0) + 1` pattern is idiomatic frequency counting in Kotlin. AI code uses it for word counting, event tallying, and histogram building."
    }
  },
  {
    "id": "ch16-q9",
    "topic": "sortedBy",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val names = listOf(\"Charlie\", \"Alice\", \"Bob\")\nval sorted = names.sortedBy { it.length }\nprintln(sorted)",
    "question": "What does this print?",
    "choices": [
      "[Alice, Bob, Charlie]",
      "[Bob, Alice, Charlie]",
      "[Charlie, Alice, Bob]",
      "[Alice, Charlie, Bob]"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "`sortedBy` returns a new list sorted by the result of the lambda. Strings are sorted by their lengths: Bob(3), Alice(5), Charlie(7).",
      "lineByLine": [
        "`sortedBy { it.length }` — sorts by character count ascending.",
        "\"Bob\" = 3, \"Alice\" = 5, \"Charlie\" = 7.",
        "Result: [Bob, Alice, Charlie].",
        "`println([Bob, Alice, Charlie])` — prints that."
      ],
      "tip": "`sortedBy` leaves the original list unchanged and returns a new sorted copy. For descending order, use `sortedByDescending`. For in-place sorting on a mutable list, use `sortBy`."
    }
  },
  {
    "id": "ch16-q10",
    "topic": "isEmpty on collection",
    "difficulty": "easy",
    "chapter": "ch-16",
    "snippet": "val items = mutableListOf<String>()\nprintln(items.isEmpty())\nitems.add(\"hello\")\nprintln(items.isEmpty())",
    "question": "What does this print?",
    "choices": [
      "true then true",
      "false then false",
      "true then false",
      "false then true"
    ],
    "correctIndex": 2,
    "explanation": {
      "summary": "`isEmpty()` returns `true` when the collection has no elements. After adding an item, it returns `false`.",
      "lineByLine": [
        "`items` starts empty → `isEmpty()` = true.",
        "`items.add(\"hello\")` — list now has one element.",
        "`isEmpty()` = false.",
        "Output: true then false."
      ],
      "tip": "`isEmpty()` and `isNotEmpty()` are used constantly in AI code as guard conditions: `if (queue.isEmpty()) return`, `if (errors.isNotEmpty()) showErrors()`. They're clearer than `size == 0`."
    }
  }
]